/*
 * Editor client script for DB table replace
 * Automatically generated by http://editor.datatables.net/generator
 */

(function($){
	var DTypeEditor; // use a global for the submit and return data rendering in the examples

	$(document).ready(function() {
		DTypeEditor = new $.fn.dataTable.Editor( {
			ajax: "php/table.codesDType.php",
	        table: "#codesDType",
	        fields: [ {
	                label: "Directive Type",
	                name: "meaning"
	            }
	        ]
	    } );
	 
	    // Activate an inline edit on click of a table cell
	    $('#codesDType').on( 'click', 'tbody td:not(:first-child)', function (e) {
	    	DTypeEditor.inline( this );
	    } );
	 
	    $('#codesDType').DataTable( {
			searching: false,
			paging: false,
			info: false,
	        dom: "Tfrtip",
	        ajax: "php/table.codesDType.php",
	        columns: [
	            { data: null, defaultContent: '', orderable: false  },
	            { data: "meaning" }
	        ],
	        order: [ 1, 'asc' ],
	        tableTools: {
	            sRowSelect: "os",
	            sRowSelector: 'td:first-child',
	            aButtons: [
	                { sExtends: "editor_create", editor: DTypeEditor },
	                { sExtends: "editor_remove", editor: DTypeEditor }
	            ]
	        }
	    } );
	} );
}(jQuery));

